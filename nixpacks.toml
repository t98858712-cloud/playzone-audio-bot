[phases.setup]
nixPkgs = ["python313", "ffmpeg", "aria2"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python bot.py & uvicorn site:app --host 0.0.0.0 --port $PORT"
